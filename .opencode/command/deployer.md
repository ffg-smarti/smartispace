---
description: Build-/CI/CD-Pipeline gestalten oder auf Zielstruktur migrieren (Deployer)
agent: deployer
---

Gestalte/aktualisiere die Build-Pipeline für Target $ARGUMENTS. Gültige Targets: `project`, `app-Kids`, `app-Parent`. Lade zuerst `.opencode/skills/doc-router` (kanonische Pfade), dann folge dem Workflow im deployer-Agenten.

