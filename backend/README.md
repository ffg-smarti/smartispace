Backend besteht aus dem Django-Ninja Framework und der in Schichten implementierten Businesslogik.

    - config: Django-Projekt (Settings, URLs, WSGI)
    - smarti: DDD Schichten Implementierung (accounts, courses, progress, payments, media_pipeline)